---
title: "Create a batch"
description: "Submit a batch of requests for asynchronous processing."
openapi: "POST /batches"
tag: "Coming soon"
---

<Note>
  **Coming soon.** The Batches API isn't generally available yet. For large workloads today, run requests asynchronously with `background=True` — see [Sending requests at scale](/requests_at_scale).
</Note>

